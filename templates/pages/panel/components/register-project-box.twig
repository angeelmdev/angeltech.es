{{ form_start(form, {'attr': {'class': 'max-w-md mx-auto space-y-5'}}) }}
    {{ form_row(form.title) }}
    {{ form_row(form.description) }}
    {{ form_row(form.url_github) }}
    {{ form_row(form.url) }}
    <!-- {{ form_row(form.image) }} -->
    <div class="flex justify-end">
        <button type="submit" class="w-40 text-white bg-blue-600 hover:bg-blue-700 font-medium rounded-lg text-sm px-5 py-2.5 text-center">
            Guardar
        </button>
    </div>
{{ form_end(form) }}
