<nav class="fixed top-0 left-0 w-full bg-white text-black shadow-lg border-b border-gray-200 px-6 py-2.5 z-50">
  <div class="flex justify-between items-center max-w-screen-xl mx-auto">
    <!-- Título -->
    <span class="text-xl font-semibold">Panel de administrador</span>

    <!-- Botón cerrar sesión -->
    <a href="{{ path('app_logout') }}"
       class="text-black hover:text-red-600 transition-colors duration-300">
       Cerrar sesión
    </a>
  </div>
</nav>
